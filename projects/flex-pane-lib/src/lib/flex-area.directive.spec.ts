import { FlexAreaDirective } from './flex-area.directive';

describe('FlexAreaDirective', () => {
  it('should create an instance', () => {
    const directive = new FlexAreaDirective();
    expect(directive).toBeTruthy();
  });
});
